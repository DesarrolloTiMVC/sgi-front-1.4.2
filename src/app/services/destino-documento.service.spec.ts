import { TestBed } from '@angular/core/testing';

import { DestinoDocumentoService } from './destino-documento.service';

describe('DestinoDocumentoService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
   expect(1+1).toBe(2)
  });
});
